tls:
  enabled: false

listeners:
  kafka:
    port: 9093
    authenticationMethod: sasl
    tls:
      enabled: false
      cert: default
      requireClientAuth: false
  admin:
    tls:
      enabled: false
  schemaRegistry:
    tls:
      enabled: false

external:
  domain: customredpandadomain.local

statefulset:
  replicas: 1
  initContainers:
    setDataDirOwnership:
      enabled: true
storage:
  persistentVolume:
    enabled: false

resources:
  cpu:
    cores: 1
  memory:
    container:
      max: 2Gi