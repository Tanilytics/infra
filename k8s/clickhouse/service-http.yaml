apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  namespace: tanilytics-storage
spec:
  selector:
    app: clickhouse
  ports:
    - name: http
      port: 8123
      targetPort: 8123
  type: ClusterIP